# Demo — H2
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:medicollab_record;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql.init.mode: always
  sql.init.schema-locations: classpath:schema.sql
server.port: 8083
