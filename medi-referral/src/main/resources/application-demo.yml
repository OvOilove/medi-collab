spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:medicollab_referral;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql.init.mode: always
  sql.init.schema-locations: classpath:schema.sql
  rabbitmq:
    host: 127.0.0.1
    port: 5672
server.port: 8084
